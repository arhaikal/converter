require 'rails_helper'

feature 'FixerApi' do
  feature 'adds api to db' do
    scenario 'succesfully' do
      # httparty = double("HTTParty")
      # allow(httparty).to receive(:get).and_return(100)
      # allow(FixerApi.add_api_to_db(Date.today)).to receive(FixerApi.make_api_call(Date.today))
    end
  end
end
